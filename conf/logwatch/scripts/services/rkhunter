use strict;
use warnings;

my @last_lines;
my $max_lines = 17;

while (my $line = <STDIN>) {
    push @last_lines, $line;
    shift @last_lines if @last_lines > $max_lines;
}

print @last_lines;

exit(0);
